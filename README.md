# VITAL-SYNC

用手機隨手記血壓與體重，資料直接存進**你自己的 Google 試算表**。

👉 **[開始使用](https://pp771007.github.io/vital-sync/)**

不用註冊、不用安裝、沒有廣告。打開網頁用 Google 帳號登入就能開始記，資料全部在你自己的雲端硬碟裡，想看原始數字隨時打開試算表。

## 怎麼開始

1. 打開 [網站](https://pp771007.github.io/vital-sync/)，按 **使用 Google 帳號登入**
2. 按 **允許存取試算表**，在 Google 的授權畫面按同意
3. 系統會自動在你的雲端硬碟建立一份叫 **VITAL-SYNC** 的試算表，之後每一筆紀錄都存在裡面

登入過一次，之後打開網頁就直接進到輸入畫面。

## 裝到手機桌面

在手機瀏覽器打開網站後，選瀏覽器選單裡的 **加入主畫面**（iPhone 是分享 → 加入主畫面）。之後就像一般 App 一樣，點圖示直接開，不用再找網址。

## 怎麼記錄

**血壓**：依序填收縮壓、舒張壓、心跳。數字填滿會自動跳到下一格，按退格鍵可以跳回上一格。數字旁邊會即時顯示這個數值屬於哪一級（正常、前期、一期⋯），輸入框的外框也會跟著變色。

**體重**：整數和小數分開兩格，例如 65.4 就是左邊打 `65`、右邊打 `4`。

兩種紀錄都可以加一句備註（例如「飯後」「運動完」），按數字下方的「▶ 新增備註」展開。

**切換頁面**：點上方的「血壓測量 / 體重紀錄」，或直接左右滑動畫面。

**看歷史**：按下方的 **歷史紀錄**，會列出最近 20 筆，每筆右邊的 × 可以刪掉（刪了救不回來）。

**看全部資料**：按下方的 **雲端試算表**，直接開啟 Google 試算表，所有紀錄都在裡面，可以自己排序、畫圖表、匯出。

## 血壓分級對照

| 等級 | 收縮壓 | 舒張壓 |
|---|---|---|
| 偏低 | < 90 | < 60 |
| 正常 | 90 – 119 | 60 – 79 |
| 前期 | 120 – 139 | 80 – 89 |
| 一期 | 140 – 159 | 90 – 99 |
| 二期 | ≥ 160 | ≥ 100 |

心跳：< 60 偏慢、60 – 100 正常、> 100 偏快。

> ⚠️ 這些數值只是自我記錄的參考，不能取代醫師診斷。身體不舒服請看醫生。

## 我的資料會去哪裡

**只會去你自己的 Google 雲端硬碟。** 這個網站沒有自己的伺服器，也沒有資料庫，你按下儲存的瞬間，資料是從你的手機直接送到你的試算表，開發者看不到、也拿不到。

- 網站要的權限只有一種：**存取它自己建立的檔案**。你雲端硬碟裡的其他檔案、照片、信件，它都讀不到。
- 那份 `VITAL-SYNC` 試算表就是你的正本。可以改檔名、搬資料夾、自己排序或做圖表，網站照樣找得到它。
- 想收回權限：到 [Google 帳戶的「與第三方應用程式和服務的連結」](https://myaccount.google.com/connections) 移除。
- 想刪光資料：直接把那份試算表丟進垃圾桶。

完整說明：[隱私權政策](https://pp771007.github.io/vital-sync/privacy.html)｜[服務條款](https://pp771007.github.io/vital-sync/terms.html)

## 常見問題

**換手機、換電腦還看得到嗎？**
可以。資料在 Google 試算表裡，用同一個 Google 帳號登入就看得到。

**沒有網路可以用嗎？**
裝到桌面後沒網路也打得開，但沒辦法上傳紀錄——要記得等有網路再存一次。

**我可以自己在試算表裡改數字嗎？**
可以，那是你的檔案。只要別改掉分頁名稱（「血壓紀錄」「體重紀錄」）和第一列的欄位標題就好。

**歷史紀錄為什麼只有 20 筆？**
那只是快速查看用的。全部紀錄都在試算表裡，按「雲端試算表」就看得到。
